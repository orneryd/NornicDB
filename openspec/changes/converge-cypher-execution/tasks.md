## 1. Establish TCK and issue baseline

- [x] 1.1 Pin official TCK revision/checksum/license and record the complete feature/scenario/step inventory.
- [x] 1.2 Implement Go step bindings, typed result/error comparison and observable side-effect checks with negative-control tests.
- [x] 1.3 Run fresh-fixture autocommit and explicit-transaction scenarios over the production Bolt server; consume results and verify transaction completion.
- [x] 1.4 Pin a Neo4j patch image/digest and validate the runner and fixed differential corpus.
- [x] 1.5 Import every reproduction/variant for the 35 scoped issues, including #452's comment; record exact TCK matches or local-only coverage.
	- Progress: issue-specific local regressions now cover #447's UNWIND/OPTIONAL MATCH result/null matrix, #449's hidden first/secondary sort keys with LIMIT, #450's property/alias collision and controls, #451's ordered WITH aggregation and control, #452's bare-UNWIND ordering/collect cases plus the later comment case, #453's list-comprehension filter/projection variants, #454's division/modulo/power/type cases including WITH, #455's relationship MERGE ON CREATE/ON MATCH persistence/readback, #456's REMOVE/label SET with count and property readback, #457's correlated CALL outer-company scope, #458's date/duration component access through WITH, #459's hidden `toLower` ORDER BY with LIMIT, #460's reduce after WITH with the standalone control, #461's single-autocommit relationship visibility through pattern reads, MATCH SET, and persistent reopen, #462's relationship property arithmetic/string expressions with readback, #463's aggregate arithmetic after AVG, #464's aggregate alias through WITH ORDER BY, #465's startNode/endNode property access with a WITH-bound control, #466's selected/computed/all-property map projections, #467's nested WITH-map access, keys, and parameter control, #468's inline collected-list slice/WITH-alias slice/subscript, #469's OPTIONAL MATCH collection size and empty-list row, #471's round precision plus ceil/floor/round float result types, #474's expression-valued `SET +=` map plus direct property-assignment control, #475's stored-list projection/subscript/head reads and predicates, #476's persisted datetime comparison vs string control and ordering, #477's lexical string min/max with numeric control, #478's list-subscript inline MATCH/MERGE with map-row control, and #479's SET followed by `RETURN sum()` with repeated updates and stored readback. Existing `TestSetRoutesConverge` covers #470 null-removal behavior across Memory/server stacks and autocommit/explicit transactions. The available local cases are verified; exact upstream TCK mappings are recorded in the machine-readable ledger `testing/cypher/tck/testdata/issues.json`, self-validated by `TestIssuesLedger_*` (matrix completeness, reproduction-ID existence in `pkg/cypher`/`pkg/storage`, and scenario-reference existence in the vendored corpus); families without an attached scenario remain "family" status until exact IDs are attached.
	- Additional verified local case: #480 chained `SET a:Extra:Hot` matches the comma form and is idempotent in autocommit and explicit transactions (`TestGH480_ChainedLabelSetMatchesCommaForm`).
	- Additional verified local case: #481 `OPTIONAL MATCH ... count(r)` groups per outer `x` (zero-count row for misses) in both modes, with non-aggregated control and pattern-comprehension workaround agreement (`TestGH481_OptionalMatchCountGroupsPerOuterRow`).
	- Exact TCK matches verified against the vendored corpus (revision `370fe27f`): #470 → `clauses/set/Set2.feature` [1][2][3] (SET property to null removes it); #477 → `expressions/aggregation/Aggregation2.feature` [7][8] (min/max over strings); #464 → `clauses/return-orderby/ReturnOrderBy6.feature` [2] (returned alias inside an aggregation ORDER BY item); #450 → `clauses/with-orderBy/WithOrderBy4.feature` [7] (alias shadows an existing variable); #481 → `clauses/match/Match8.feature` [2] (counting rows after MATCH, MERGE, OPTIONAL MATCH). Local-only is recorded with reasons where the corpus demonstrably lacks the shape: no `round(` anywhere (Neo4j-extension #471), no CALL {} subquery (Neo4j-extension #457), no map projections (Neo4j-extension #466), no startNode/endNode property access (#465), plus storage/concurrency/perf invariants (#448/#461/#487).
- [x] 1.6 Check in exact known failures; fail on new/changed failures, missing scenarios, unexpected passes and harness errors.
	- The checked-in ratchet (`testing/cypher/tck/testdata/ratchet.json`) records all 7794 scenario/mode outcomes (100% pass at `370fe27f`) and `compareRatchet` fails on regressions, changed failure signatures, stale gaps now passing, harness errors, missing scenarios and untracked scenarios. CI enforces it through `make cypher-conformance` (`cypher-tck-ratchet` + `cypher-tck-vetted`) with negative-control unit tests. Per owner direction no further ratchet work is planned.
- [x] 1.7 Add required CI workflow, artifacts and runnable make targets; configure required-check enforcement during rollout.
	- `cypher-conformance.yml` triggers on `pkg/**`, `testing/cypher/**`, runner scripts, plan docs and OpenSpec changes; runs `make cypher-conformance` and `make cypher-differential` and uploads diagnostics with `if: always()`; make targets exist. Owner decision: required-check branch-protection enforcement will not be configured; the workflow remains the gate.
- [ ] 1.8 Record test/coverage/benchmark baseline and regenerate the divergence candidate ledger with reproducible inputs.
	- Owner decision: the existing `docs/plans/reports/DIVERGENCE_REPORT.md` / `HARD_CONVERGENCE.md` are used as-is to guide convergence; graphify regeneration is not being set up. The convergence work is tracked per report section below (see 3.3/8.1 progress).

## 2. Share execution context and lexical contracts

- [ ] 2.1 Extract common preparation while retaining public/internal cache, limits and transaction differences.
		- Progress: top-level and internal execution share duplicate RETURN column validation (`TestExecuteInternal_RejectsDuplicateReturnColumns`). Internal fragments still need inherited scope-aware preparation; applying the full top-level semantic validator rejects valid correlated subqueries.
- [ ] 2.2 Introduce bound scope, inherited parameters, source spans and cancellation propagation without text substitution.
- [ ] 2.3 Converge quote/comment/bracket-aware scanning and prove complete shape/fragment consumption.
- [ ] 2.4 Add typed dispatch outcomes, effect-boundary tests and unresolved-expression instrumentation.

## 3. Repair mutations and storage visibility

- [x] 3.1 Reproduce then fix #462/#474 through scoped assignment and recursively evaluated map values; enforce rollback on evaluation failure.
- [x] 3.2 Reproduce then fix #455/#456/#470/#480; verify persisted graph, MERGE branches, null removal and label updates.
- [ ] 3.3 Build storage contract tests across Memory, Badger, production wrappers and transaction/multidb views.
	- Progress (DIVERGENCE_REPORT.md §2, storage stack): closed the 22 partially-forwarded capability gaps on the production chain. `pkg/storage/wrapper_capabilities.go` declares the capability interfaces plus compile-time assertions for Badger/WAL/Async/Namespaced/Memory across `NodeProjectionReader`, `NodeIterator`, `EmbeddingCountProvider`, `EmbeddingUpdater`, `NamespaceLister`, `NamespaceSchemaProvider`, `PrefixStatsEngine`, `NamespaceLabelStatsProvider`, `StartupMaintenanceStateEngine`, `MVCCMaintenanceEngine`, `TemporalMaintenanceEngine` and `StorageEventNotifier`; a missing forward now fails the build. New forwards: `GetNodeProjected` on WAL (delegate/materialize) and Async (overlay-aware), maintenance/counting/schema/iteration/embedding forwards plus namespace-translating `On*` event registration on NamespacedEngine. `TestWrapperCapabilityParity_*` contract tests cover projected reads across the whole stack (including the async overlay), namespace-scoped prefix/label counts, `IterateNodes` prefix stripping, `UpdateNodeEmbedding` ErrNotFound/no-orphan behavior, shutdown-marker round-trip, and callback ID translation/filtering. Multidb: `sizeTrackingEngine` already forwards the full set (report cell stale); `CompositeEngine` gained the 19 missing capabilities in `pkg/storage/composite_capabilities.go` — MVCC visibility/head reads route by ID, indexed visibility merges constituents, label-ID iteration dedupes, `UpdateNodeEmbedding` routes, mutation-version/embedding-count/namespace reads aggregate, maintenance and lifecycle operations broadcast, and `On*` registration broadcasts — backed by `TestCompositeCapabilityParity_*` and composite compile-time assertions. Only the 12 forwarded-by-nobody Badger methods remain.
- [ ] 3.4 Reproduce then fix #461 and #448; verify atomic publication, embedding flush visibility, reopen and any existing-data repair needs.
	- #461 progress: `TestGH461_AutocommitCreatedRelationshipVisibleAcrossTransactionsAndReopen` verifies one auto-commit CREATE containing nodes+edge, six snapshot pattern forms, subsequent implicit MATCH SET/readback, and persistent reopen. The legacy existing-record repair assessment remains open.
	- #448 progress: root cause was `UpdateNodeEmbedding` staging the write-back in `nodeCache` without maintaining the cache-side label index, so `GetNodesByLabel`/`NodeCountByLabel`/`StreamNodesByLabelProjected` skipped engine label-index rows shadowed by the cache — whole in-flight batches vanished from scans until flush. Fix: embedding write-backs now maintain the label index via `syncNodeLabelIndexForEmbeddingLocked` (idempotent label add when labels are unchanged, full re-sync only on label divergence). Regression `TestGH448_StagedEmbeddingWritebackKeepsLabelScanComplete` covers 64 committed nodes with one staged 32-node batch: complete scans/counts/projected reads pre-flush, staged-embedding visibility, and post-flush persistence. New `BenchmarkAsyncEngine_UpdateNodeEmbedding` measured the staged path: full-sync implementation 2160 ns/op 15 allocs → targeted helper 617 ns/op 10 allocs (3.5x faster) on M2 Max; profile confirms no bucket-scan frames remain on this path.
- [ ] 3.5 Normalize typed property access and capability forwarding; cover #475's storage leg and individual/batch mutation equivalence.
	- Progress (individual/batch mutation equivalence): `TestMutationEquivalence_*` runs over Badger, WAL, Async, WAL+Async and Namespaced+WAL+Async stacks, asserting single-vs-bulk node creates, edge creates, deletes and single-vs-staged updates leave byte-equivalent stored state (labels, typed property graphs with nested lists/maps and mixed-type lists, endpoints). The async staged path flushes as bulk operations, so these pin the overlay/direct convergence. Full storage suite ×2 green.
- [ ] 3.6 Verify own writes, stable snapshots, rollback, authorization, cache isolation and cancellation.

## 4. Proper errors for unhandled queries

Owner decision: no alternate execution path will be added.
Unhandled queries must fail like Neo4j fails them, through the converged pipeline.

- [x] 4.1 Route valid-but-unhandled statements to a single terminal unsupported/syntax error in the converged execution pipeline; never a silent success, alternate text executor, or re-dispatch.
	- `executeWithoutTransaction` is the single router (`executeQueryAgainstStorage` is a thin view-swap); its default case is the one terminal. It now carries Neo4j's syntax-error classification (`Neo.ClientError.Statement.SyntaxError`, `UnexpectedSyntax`) via `classifiedCypherError` while keeping the localized message. The Nornic validator's valid-start and unclosed-quote terminals got the same classification.
- [x] 4.2 Add a regression test for every found valid-but-unhandled query: assert the proper error class, no observable effects, and behavior parity across autocommit and explicit transaction modes.
	- `TestUnhandledStatementsReturnClassifiedSyntaxErrors` covers the found shapes (`SHOW WHATEVER` router terminal, `WHATEVER 1` start terminal) on memory and server stacks in both modes, asserting the Bolt classification, nil result and unchanged node count.
- [x] 4.3 Verify the same error class and rollback semantics are returned from every route/mode (server, executor, transaction wrapper).
	- The 4.2 matrix runs through `Execute` (executor + explicit-transaction wrapper + namespaced server stack); each case asserts the same `Neo.ClientError.Statement.SyntaxError` class and that COMMIT afterward succeeds with no effects. Existing `TestExecuteQueryAgainstStorage_DispatchBranches` continues to pin the terminal message at the direct-router level.

## 5. Converge expressions

- [ ] 5.1 Fold computed-row and predicate evaluators into shared typed semantics; retain measured compiled fast leaves.
  - Progress: `evaluateCaseExpressionFromValues` and `evaluateConditionFromValues` delegate to the shared evaluators via `withValueBindings` (~130-line clones removed; undefined identifiers evaluate to null). `evaluateExpressionFromValues` is now a 40-line facade over the shared evaluator: direct `values[expr]` lookup (the call-tail plans pre-store aggregate results under the expression text), a scoped compatibility shim for the legacy "properties" sub-map precedence on projected node maps, relationship-pattern and lone-arrow fragment round-trip guards, then `evaluateExpressionWithContextDefined` with value bindings + entity scopes, and the legacy terminal contract (unrecognized expression text round-trips so callers can raise proper statement errors or route to EXISTS-subquery machinery). The 353-line parallel evaluator and the `evaluateArithmeticExprFromValues` helper are deleted (−369/+156 net). A scratch differential harness over 125 computed-row expressions verified parity for every recognized shape; the only intentional semantic changes are the removals of expression-text fallthrough (`missing` → null, `size(null)` → null, comparisons/`head()`/`coalesce(nilVal,5)` now evaluate properly). Shared handlers gained the computed-row map support the legacy evaluator had: `length`/`nodes`/`relationships` read path maps (`pathValueParts`), and the fn package's `id`/`elementId`/`labels`/`properties` resolve projected node maps. Pinned tests updated to converged semantics (size(null)), full cypher suite green (~35.7s). Benchmarks: condition 345–349 ns/op (68 B, 2 allocs), CASE 544–548 ns/op (220 B, 5 allocs), new `BenchmarkEvaluateExpressionFromValues` 1043–1049 ns/op (348 B, 5 allocs); a CPU profile shows all work in the shared key-scan pipeline (`findTopLevelOperator`, operator scans, postfix-access scan). Remaining for later slices: the `properties()`-sub-map precedence shim could move into `splitPostfixPropertyAccess` if the row evaluator aligns, and the Defined-recognition operator scan runs once per expression before the shared evaluator re-scans (a candidate fast leaf).
  - Progress (HARD_CONVERGENCE.md item 5, read-only twins first): `BadgerEngine.NodeCountByPrefix`/`EdgeCountByPrefix` now share the `countByPrefix(prefix, keyType, cache)` kernel, and `namespaceForNodeIDs`/`namespaceForEdgeIDs` share the generic `namespaceForIDs[T ~string]` kernel with identical error text; each public twin is a thin wrapper. `BenchmarkBadgerEngine_(Node|Edge)CountByPrefix_Warm` measured 20.9–21.1 ns/op, 0 allocs before and 21.5–21.9 ns/op, 0 allocs after (within noise) on M2 Max. `BadgerTransaction.mergePendingNodesLocked`/`mergePendingEdgesLocked` now share the generic `mergePendingLocked[ID comparable, T any]` kernel (pending shadows committed, deleted drops, copy-on-escape preserved; nil records skipped via zero-ID), and the node twin gains the edge twin's no-overlay fast path. Existing `TestTxReads_MergePending*` pass unchanged; new `BenchmarkTxReads_MergePending*` records no-overlay 2.4 ns/op 0 allocs and overlay 48.6–50.2 µs/op 1028 allocs (nodes) / 34.4–35.5 µs/op 516 allocs (edges) per 512 records. `coercePathNodes`/`coercePathRels` (bolt packstream) now share the generic `coercePathRecords[T any]` kernel ([]*T, []T, and mixed []any inputs normalize identically); existing coverage assertions extended with the value/pointer slice forms. The node/edge decay filters now share `filterEntityByDecay` and the node/edge property decay filters share `filterPropertyByDecay` (entity-specific scoring selected by an isEdge branch so the hot scan path stays allocation-free); `BenchmarkStreamNodes_(Full|ByPrefix)` unchanged within noise (1.59–1.61 ms/op 16806 allocs and 1.12–1.13 ms/op 15767 allocs). Constraint DDL parsing (HARD_CONVERGENCE.md item 6, first step): the six `CREATE CONSTRAINT … FOR … REQUIRE` prologues are now one `parseCreateConstraintForRequireHead` helper (shared empty/prefix/FOR/REQUIRE/trailing-OPTIONS errors, optional name, pattern span, require expression); `parseCreateConstraint{ForRequire,Cardinality,Policy,RelationshipKeyOrCompositeUnique,NodeKey,Type,SimpleProperty}DDL` use it for their FOR branches (−201/+60 lines in `pkg/cypher/schema.go`, identical error text, full cypher suite green). The remaining per-shape predicate parsing and the ON/ASSERT branches stay shape-specific. Remaining twin groups (decode/coerce pairs, MVCC version-record loaders) stay open. The router/evaluator/§1–§3 report items were verified already collapsed (single router, evaluator funnels through WithContextFull); §2's 12 Badger-only methods are by-design unwrap-to-Badger operations (callers hold the concrete type).
- [ ] 5.2 Fix list comprehension, numeric, reduce, postfix access, map and temporal issue families in all expression positions.
  - Progress: compound CASE operands fixed in all expression positions. The shared operator scanners stay CASE-unaware (keeping the hot scan allocation-free); `evaluateExpressionWithContextFull` now detects CASE blocks once per expression node via `caseBlockSpans` (zero-alloc when absent) and substitutes each with its evaluated literal (`evaluateExpressionWithCASESubstituted`), so `acc + CASE WHEN x > 1 THEN x ELSE 0 END`, reduce over CASE reductions and CASE inside comparisons evaluate correctly. The row evaluator delegates compound CASE-containing expressions to the shared path before its CASE-unaware comparison chain, and `leadingCaseExpressionEnd` was fixed to require a LEADING CASE keyword (it previously matched a CASE anywhere, which misrouted reduce-with-CASE into a truncated parse). Benchmarks unchanged within noise (see §5.1 numbers); a rejected experiment made `findTopLevelOperator` CASE-aware per scan (+27–55% on the evaluator benchmarks) and was reverted in favor of the once-per-node substitution.
- [ ] 5.3 Propagate typed errors through all migrated callers; remove expression-text and implicit-null error fallthrough.
  - Progress: the row-level `coalesce` now matches the shared/fn semantics — undefined operands behave as null (skip to the next argument) and `EXISTS { … }` subqueries evaluate as non-null booleans via `evaluateRowExistsPredicate` (previously `coalesce(missing, null, 5)` raised a parse error and `coalesce(EXISTS { … }, 5)` returned 5 for every row).
- [ ] 5.4 Verify CASE/COALESCE short-circuiting, null/undefined distinction, scope and parameter handling.
  - Done: new `pkg/cypher/case_coalesce_convergence_test.go` pins, at the Execute level (division-by-zero as the eager-evaluation canary) and the evaluator level: searched/simple CASE skip unmatched branches and ELSE, coalesce stops at the first non-null, undefined identifiers are null (IS NULL true, comparisons null, not truthy), CASE without ELSE is null, UNWIND/reduce scopes shadow correctly, `$param` handling in conditions/test expressions, and missing parameters fall through coalesce. All green; full cypher suite green (~35.6s).

## 6. Converge composition

- [ ] 6.1 Fix aggregate discovery/grouping, projection aliases, ordered collections and full sort-key evaluation.
- [x] 6.2 Fix UNWIND/OPTIONAL MATCH cardinality and correlated CALL; migrate UNION/FOREACH to bound child contexts.
		- Progress: the reported FOREACH composed-write regressions are covered by `TestForeach_ComposedWritesRetainBindings` and `TestForeach_ComposedMutationShapes`. First migration step (bound-child property parsing): `parsePropertyValue` now resolves a bare identifier through the value scope (`boundValue`) before its literal-string fallback, and `resolveCreatePropertyReferences` extends dotted-reference roots (`x.key`) to the value scope — `CREATE (n {v: x})`, `CREATE (n {v: row.name})` and `MERGE (m {id: x})` all resolve bound loop variables with their real Go values, pinned by `TestCreatePropertyValues_ResolveBoundLoopVariables` (including the unbound-identifier historical literal-string behavior). Unchanged perf: `BenchmarkEvaluateExpressionFromValues` 1,125–1,133 → 1,132–1,143 ns/op (noise) and `BenchmarkPipelineCreateBound/with_create_rel` 113.7–113.8 → 112.8–115.4 µs/op (noise). Cypher suite green (35.3s). Second step (the flip): `executeForeachWithContext` now executes the update clause per item through a bound child context (`withValueBindings(ctx, {variable: item})`) — MERGE keeps its entity contexts, every other clause shape goes through `executeInternal` with the same clause text — and no longer calls `replaceVariableInQuery` (still used by the UNWIND mutation path). `TestForeach_BoundExecutionPinsNoSubstitution` pins quote-heavy string values (`O'Brien`, `a;b`), structured map items via dotted access and typed integers; existing FOREACH batteries pass unchanged. `BenchmarkForeach_CreatePerItem` (25 CREATEs/op, memory engine) measures 1.14–1.31 ms/op after vs 1.27–1.61 ms/op before (overlapping bands, allocs identical 12,300) — within noise. Cypher suite green (35.3s). Third step (bound-entity DELETE): standalone `DELETE <var>` / `DETACH DELETE <var>` with the variable bound through the value scope now deletes the entity — `tryExecuteBoundStandaloneDelete` resolves `*Node`/`*Edge`/string-ID targets, routes non-DETACH node deletion through the same `validateNoResidualRelationships` guard as the MATCH path, and keeps the historical classified match-required error for unbound targets; the router's standalone-DELETE flag now accepts position 0 (`>= 0`) so plain `DELETE x` reaches the delete executor instead of the terminal unsupported-type rejection. `TestForeach_BoundStandaloneDelete` pins node/edge/string-ID deletion, the residual guard and the unbound error; `BenchmarkForeach_BoundStandaloneDelete` pins the new path at 934 µs/op (dominated by per-op executor/engine setup). Cypher suite green (35.4s). Fourth step (UNWIND per-item loop): the `UNWIND … MATCH …` per-item fallback now executes each item with a bound child context (`withValueBindings`) plus the parameter, instead of `replaceVariableInQuery` text substitution — MATCH property maps and expressions resolve the binding through the same scope as CREATE/MERGE. With no callers left, `replaceVariableInQuery` and its three direct tests (and the nested-UNWIND substitution internal test) are deleted (−190 lines incl. tests); `replaceVariableInMutationQuery` (the UNWIND mutation paths, 4 call sites) remains for its own migration. `TestUnwind_MatchedPerItemBoundExecution` pins hostile values (`O'Brien;2`, `a, b`) through the per-item MATCH path; existing UNWIND batteries and `TestPostSubst_*` (literal-shape robustness guards) pass unchanged. `BenchmarkUnwindMergeBatch_OriginalTextUpsert` 317–322 µs/op before vs 319 µs/op after (clean samples) and `BenchmarkUnwindBenchHopLinkBatch` 661–701 µs/op before vs 707 µs/op after — within noise. Cypher suite green (35.5s). Fifth step (UNWIND mutation paths): all five remaining substitution sites (four in the UNWIND mutation loops in `clauses.go`, one in `executeUnwindMergeChainBatch` in `merge.go`) now execute the un-substituted clause against a bound child context — the WITH/UNWIND row-alias "{}"-collapse and skip-bare-replacement guards are gone with the helper. `replaceVariableInMutationQuery` and its `sortedMapKeysByDescendingLength` sorter are deleted (zero callers); `replaceIdentifierOutsideQuotes` and `valueToCypherLiteral` remain for other callers. `TestUnwind_MutationBoundExecutionHostileValues` pins `O'Brien;2`/`a, b` plus typed integers through MERGE/ON CREATE SET over unwound rows; the full UNWIND/seeder batteries pass unchanged. `BenchmarkUnwindMergeBatch_OriginalTextUpsert` 325.7–330.4 µs/op before vs 322.4–325.4 µs/op after and `BenchmarkUnwindBenchHopLinkBatch` 642.6–679.6 µs/op before vs 647.7–721.3 µs/op after — overlapping bands, allocs byte-identical (2318/6297–6299). Cypher suite green (35.8s). Sixth step (UNION bound contexts): `executeReturn` now seeds its row scope from the ctx value bindings (innermost bindings shadow params), so UNION/CALL branches resolve outer bound values — bare identifiers, property access and arithmetic — without text re-entry; contexts without bindings short-circuit at zero cost. `TestUnion_BoundContextResolvesBranchValues` pins branch resolution (`RETURN x`, `x + 1`, `row.name`) and CALL-subquery UNION composition; `BenchmarkExecuteReturn_NoBindings` measured 1,276 ns/op, 496 B, 16 allocs before vs 1,104 ns/op, 496 B, 16 allocs after (no regression) and `BenchmarkExecuteReturn_BoundValues` pins the bound path at 2,805 ns/op, 1,029 B, 29 allocs. Cypher suite green (35.1s). §6.2 is complete: FOREACH, UNWIND (read and mutation) and UNION all execute through bound child contexts; both query-text substitution helpers are deleted.
- [ ] 6.3 Complete #447/#449–#452/#457/#459/#463/#464/#468/#469/#478/#479/#481 regressions and applicable TCK cases.
- [ ] 6.4 Preserve write/read barriers and prohibit LIMIT from skipping required writes, sorting or grouping.

## 7. Stream snapshot reads

- [x] 7.1 Implement projected snapshot-visible iterators including pending mutations and early termination.
		- Verified for Badger transactions, Async, WAL, Composite, and Namespaced wrappers; tests cover pending updates/deletes, begin-snapshot replay, projection, deduplication, and early stop.
- [x] 7.2 Reproduce #487 and demonstrate per-shape latency/allocation improvements with visit counters and profiles.
	- Verified on Apple M2 Max with repeated `BenchmarkStatementRouting` runs: `simple_match_limit` 13.0 µs / 6.0 KB / 112 allocs in explicit tx vs 23.6 µs / 11.4 KB / 185 allocs autocommit; `property_lookup` 295 µs / 301 KB / 2,264 allocs vs 1.11 ms / 1.34 MB / 14,225; `filter_count` 202 µs / 295 KB / 2,124 vs 833 µs / 1.30 MB / 12,109; final `one_hop_limit` 111 µs / 101 KB / 1,339 vs 129 µs / 98.6 KB / 1,328; `return_literal` 3.28 µs / 2.61 KB / 44 vs 3.76 µs / 2.21 KB / 44. Added query-level and explicit-snapshot visit-count checks. CPU profiles drove bounded transaction-local caches for MVCC adjacency IDs, resumable label prefixes, endpoint nodes, and visible edge bodies; the final profile no longer shows adjacency-index iteration or edge-version lookup among the leading application costs. Full `pkg/storage` and `pkg/cypher` suites pass. Profile timings are hardware-specific; retain the benchmark for remeasurement on target hosts.
- [x] 7.3 Verify snapshot isolation, cancellation, buffer lifetime and performance of existing correct workloads.
	- Verified by begin-snapshot replay and adjacency consistency regressions, graceful snapshot-expiration cancellation/reader-release coverage, and wrapper tests that retain projected callback nodes/properties after iteration. Bounded-prefix/endpoint/edge cache tests pass under `-race`; the five-shape #487 benchmark matrix is recorded under 7.2.

## 8. Retire remaining divergence

- [ ] 8.1 Converge non-policy DDL and relevant node/edge kernels with contract tests and benchmarks.
  - Progress (HARD_CONVERGENCE.md item 6, second step): the storage knowledge-policy profile pairs flagged at 0.94–0.96 similarity share `createKnowledgeProfile[T]` and `alterKnowledgeProfile[T]` kernels; `CreateDecayProfileBundle`/`CreatePromotionProfile` and `AlterDecayProfile`/`AlterPromotionProfile` are one-line wrappers with identical lock discipline, duplicate/IF NOT EXISTS semantics, kind-specific error constructors, copy-on-write and mutation-finish ordering, pinned by `TestKnowledgeProfileKernels_SharedContract`. Storage ×2 green (86.1s); `BenchmarkStreamNodes_(Full|ByPrefix)` unchanged within noise.
  - Progress (HARD_CONVERGENCE.md item 6, third step): the knowledge-policy DDL alter/drop family (`pkg/cypher/knowledgepolicy_ddl.go`) now shares `kpParseKnowledgeName` (name scan + profile/policy name-required error, 9 call sites), `kpParseSetOptions` (the `SET OPTIONS { … }` raw-updates block, used by `parseAlterDecayProfile`/`parseAlterPromotionProfile`/`parseAlterPromotionPolicy` with identical `SET`-vs-`OPTIONS`-missing semantics) and `kpParseDrop` (`DROP [IF EXISTS] <name>` tail for the three drop statements). New `TestPlanDDL_SharedAlterDropShapes` pins the shared error texts and command shapes across all six statements, and `BenchmarkParseKnowledgePolicyDDL_AlterSetOptions` (444–451 ns/op, 536 B/op, 11 allocs) pins the alter path. Cypher suite green (35.4s), storage green (40.4s); `BenchmarkEvaluateExpressionFromValues` 1095–1099 ns/op within the observed run-to-run band (1043–1099). Remaining item-6 follow-up: the storage `CreatePromotionPolicy`/`Drop*` guards and the MVCC twins stay open.
  - Progress (HARD_CONVERGENCE.md group 11, provenance filtered scans): the five newest-first filtered scans of `EdgeMetaStore` (`GetBySignalType`, `GetMaterialized`, `GetByTimeRange`, `GetByOrigin`, `GetBySession`) now share the `filterMetasLocked(limit, keep)` kernel — identical iteration order, limit semantics and copy-on-escape. New `TestEdgeMetaStore_FilterSemantics` pins newest-first order, limit application and copy-on-escape; `BenchmarkEdgeMetaStore_GetBySignalType` (1000-record full scan) measures 69.5–69.8 µs/op, 257.5 KB/op, 1011 allocs. Storage ×2 green (83.6s), cypher green (35.2s).
  - Progress (HARD_CONVERGENCE.md item 10, spanning tree): `bfsSpanningTree`/`dfsSpanningTree` (101/104 lines, 0.75 similarity) now share the `spanningTreeFrom(start, config, depthFirst)` frontier kernel — BFS pops the front and scans edges in list order, DFS pops the back and scans reversed so list order is still explored first; the level window, terminate labels, direction, relationship/label filters and visited set are identical. Existing spanning-tree tests pass unchanged; new `TestSpanningTreeKernel_BFSAndDFSShareFrontierInvariants` pins the shared edge sets on a tree and a cyclic graph, and `BenchmarkSpanningTreeKernel` (100-node tree) measures bfs 76.6–77.5 µs/op (1215 allocs) vs dfs 76.3–77.4 µs/op (1208 allocs) — mode parity within noise. Cypher green (35.3s), storage green (39.1s).
  - Progress (HARD_CONVERGENCE.md item 10, fulltext doc builders): `buildNodeFulltextDoc`/`buildEdgeFulltextDoc` (0.86 similarity) now share `buildFulltextDocFromProperties(properties, content)` — the property canonicalization (nil/empty skipping, lowercase keys/values, raw-value preservation) and ftDoc assembly are written once; the builders keep only their entity-specific content extractors, which genuinely differ (node: requested properties only; edge: all properties on empty list, nil-safe writes). New `TestFulltextDocKernel_NodeAndEdgeBuildersAgree` requires identical kernel output for equivalent node/edge properties; `BenchmarkFulltextDocKernel_(Node|Edge)` pin the builders at 975–984 ns/op (23 allocs) and 916–926 ns/op (21 allocs). Cypher green (34.9s), storage green (39.5s). Follow-up flake fix: the edge-content equality in `TestFulltextDocKernel_NodeAndEdgeBuildersAgree` now passes the requested properties explicitly instead of the nil list — the empty-list edge path concatenates map values in map-iteration order (nondeterministic, not part of the kernel contract); test runs 20× green.
  - Progress (DIVERGENCE_REPORT item 3, getNode variant family): `BadgerEngine.GetNode` and `GetNodeWithoutEmbeddings` (53 lines each) now share the `getNodeByID(id, decode, hitCopy, cacheStore)` kernel — cache-hit copy shape, miss decode, decay filter, not-found handling and cache-store decision are one implementation; the two reads differ only in the decode function, the hit-copy shape and whether the decoded node is cached (embedding-free reads never store, so they cannot poison the cache). New `TestBadgerEngine_GetNodeKernel_CacheStoreAsymmetry` pins the miss/hit counters, the no-store invariant and copy isolation; `BenchmarkBadger_GetNode_CacheHit` measured identical within noise before (280–290 ns/op, 497–498 B, 4 allocs) and after (278–289 ns/op, 497 B, 4 allocs). Existing `TestNamespacedEngine_GetNodeWithoutEmbeddingsSkipsSeparateVectors` passes unchanged. Storage ×2 green (81.8s), cypher green (35.4s).
  - Progress (DIVERGENCE_REPORT item 3, getEdge variant family): `BadgerEngine.GetEdge` and the MVCC current-head branch of `getEdgeVisibleAtInTxn` now share the `readEdgeBodyInTxn(txn, id)` kernel — key lookup, decode and the decay filter are one implementation, so the direct and visible-at reads cannot drift apart. Missing keys are reported as `(nil, false, nil)` so the MVCC caller keeps its record-path fall-through, while decode failures and decay-filtered edges return errors as before (the MVCC branch's redundant `decoded.ID = id` after `decodeEdgeBodyByID` was dropped — the decode already stamps the ID). New `TestBadgerEngine_GetEdgeKernel_DirectAndVisibleAgree` requires `GetEdge` and `GetEdgeVisibleAt(head.Version)` to return the identical decoded edge, identical `ErrNotFound` for missing IDs, and unchanged historical record resolution after the head moves on; `BenchmarkBadger_GetEdge_DirectRead` pins the direct read at 1,259–1,297 ns/op, 1,584 B/op, 20 allocs after vs 1,276–1,304 ns/op, 1,584 B/op, 20 allocs before (identical within noise). Storage ×2 green (84.2s), cypher green (35.4s).
  - Progress (DIVERGENCE_REPORT item 3, constraint-contract validators): `validateConstraintContractsForNodeLocked`/`validateConstraintContractsForEdgeLocked` now share the `validateConstraintContractsLocked` kernel — namespace/schema resolution, contract collection, boolean-kind filtering, evaluation and the invalid/violated error wrapping are one implementation, selected by exactly one of node/edge being non-nil (per-entry dispatch, allocation-free). Contract collection is a single deterministic pass: target order (label order for nodes, the single relationship type for edges) with each target's contracts in the schema's name-sorted order and first-occurrence name dedup — the edge path preserves its previous sorted evaluation order, and the node path gains determinism where it previously iterated a map. New `TestConstraintContracts_ValidateKernel_DeterministicOrderAndParity` pins first-violated reporting (alpha before beta by label order, flipping with label order) and identical wrapping on both paths; `BenchmarkConstraintContracts_ValidateLocked_Node` measures 359–381 ns/op, 537 B/op, 7 allocs after vs 396–437 ns/op, 537 B/op, 7 allocs before (−10–13% time, identical memory). Storage ×2 green (82.0s), cypher green (35.0s).
  - Progress (HARD_CONVERGENCE.md item 10, route registration): `registerNornicDBRoutes`/`registerAdminRoutes`/`registerGDPRRoutes`/`registerRetentionRoutes` and the authenticated subset of `registerAuthRoutes` are now `routeSpec` tables over one `registerRouteTable(mux, []routeSpec)` helper — every entry wires through `withAuth(handler, perm)`, so a route cannot be registered without its permission check and the path/permission/handler shape cannot drift between registrars. New `BenchmarkServerRouteTableRegistration` pins table wiring at 5,802 ns/op, 6,496 B, 91 allocs per six-route table (dominated by ServeMux pattern parsing, startup-only); `BenchmarkSecurityMiddleware` measured identical within noise before (1,707 ns/op, 1,928 B, 27 allocs) and after (1,711 ns/op, 1,928 B, 27 allocs). pkg/server suite green (103.8s); full `go build ./...` clean.
  - Progress (HARD_CONVERGENCE.md item 7, HNSW insertion core): `Add`/`addWithLevel0Candidates` now share the `addLocked(id, vec, level0Candidates)` kernel — the in-place upsert fast path, vector storage, arena/level bookkeeping, entry-point walk and neighbor linking are one implementation; when `level0Candidates` is non-nil, level 0 links straight from the supplied candidates (GPU build path, caller-owned, never released to the pool) and every other layer goes through `searchLayer`. `addWithLevel0Candidates` is now a thin wrapper; `Add` delegates with nil candidates. Verified allocation-neutral: the per-node hot path cannot use `util.SafeIntToUint32` (strconv.Itoa + fmt.Sscan ≈ 4 allocs/node — a micro probe with 300 vectors went 5,585 → 6,976 allocs until replaced with an allocation-free `uint64` overflow guard, then back to 5,585, byte-identical to the original `Add`); controlled A/B on `BenchmarkHNSWBuildCPUVsAcceleratedCandidates/CPU` (1x, benchmem) measures 141.0 ms/op 5.586 MB 64,064 allocs before vs 144.8–145.5 ms/op 5.560–5.600 MB 64,268–64,338 allocs after — within run-to-run noise (earlier apparent +12.9% delta was a flag misalignment plus the SafeIntToUint32 cost). pkg/search suite green (9.1–9.6s).
  - Progress (HARD_CONVERGENCE.md item 7, search-layer variant family): `searchLayerHeap` (build path, no ctx, caller IDs) and `searchLayerHeapPooledFromEntriesWithContext` (query path, ctx probes, distance items) now share the `searchLayerHeapKernel[M layerCtxMode]` expansion core — visited-state prep, candidate/result heap machinery, entry seeding, neighbor expansion and closest-first extraction are one implementation, so a recall fix like #433 can no longer land in only one copy. The context-probe policy is selected at compile time via marker types (`layerNoCtx` probes dead-code-eliminated so the build hot path stays branch-free; `layerWithCtx` probes once per candidate pop and once per 32 neighbors, as before); `wantIDs` selects idsPool vs itemsPool output once per call. Entry seeding is now shared through `seedLayerEntry` (one call per entry point, not per neighbor) and gains validity/deletion/dimension guards on the build path — the build path always passes a live entry point so no probe ever fires there. New `TestSearchLayerHeapKernel_SingleAndMultiEntryAgree` requires both wrappers to return the same closest-first candidates for the same entry point, duplicate-entry idempotence and an empty-entry guard; `TestSearchLayerHeapKernel_ContextCancellation` pins the query-path cancellation. Benchmarks within noise: `BenchmarkHNSWIndex_Add` 463,993 ns/op 100 allocs before → 450,834–455,011 ns/op 99 allocs after (−2–3%); `BenchmarkHNSWIndex_Search` 152,961 ns/op 11 allocs before → 148,913–161,435 ns/op 11 allocs after; build CPU 144.8–145.5 ms 64,268–64,338 allocs before → 144.9–148.4 ms 64,275–64,455 allocs after. pkg/search green (8.9–9.1s) incl. -race HNSW run, cypher green (35.0s).
  - Progress (HARD_CONVERGENCE.md item 8, GPU build accelerators): the three near-copies of the beam-search orchestration (`Cuda`/`Metal`/`Vulkan` `CandidateSearchGraph` + `candidateSearchGraphGroup`, 0.92–0.96 similarity) are now one `candidateSearchGraphBatched[A gpuBuildCandidateSearcher]` + `candidateSearchGraphGroup[A gpuBuildCandidateSearcher]` generic pair in `hnsw_build_gpu.go` — env-sized query grouping, beam seeding from the graph entry point, the expand→local-search→remap iteration and topK truncation are written once; only the backend-local `candidateSearch` call (one shared signature across the three accelerators, now named by the `gpuBuildCandidateSearcher` interface) remains per-accelerator. Each `CandidateSearchGraph` keeps only its backend-specific precondition/device-availability checks (−146 net lines). New `TestCandidateSearchGraphKernel_Orchestration` pins the deterministic two-iteration walk over a five-vector graph with a fake backend searcher (beam [0,2,1], frontier lens [4,4,4,4] for 3 queries/group-size 2) and `TestCandidateSearchGraphKernel_Cancellation` pins cancellation propagation. The CUDA file (linux-only) was compile-verified on darwin via a synthetic `zzcudacheck` tag run against the cuda stub bridge. Benchmarks (3x, benchmem): `AcceleratedMetal` 107.66 ms/op 11.78 MB 77,347 allocs before vs 108.08 ms/op 11.77 MB 77,377 allocs after (+0.4% time, +0.04% allocs — the path this change touches); `AcceleratedCPUShim` 142.7 ms/op 63,659 allocs before vs 147.7 ms/op 63,905 allocs after (untouched frontier path, run noise). pkg/search green (9.3s), cypher green (36.2s).
  - Progress (HARD_CONVERGENCE.md item 10, constraint contracts): the Engine/Locked evaluator pairs (`evaluateNodeConstraintContractExpression…`, `evaluateRelationshipConstraintContractExpression…`, `countMatchingPatternEdges…`) are now one implementation over the `constraintGraphView` interface (`OutgoingEdges`/`IncomingEdges`/`Node`), with `engineConstraintView` and `txConstraintView` adapters — the engine and locked-transaction call paths can no longer drift apart (−95/+? net in `constraint_contracts.go`). Public wrapper signatures unchanged. New `TestConstraintContractEvaluation_EngineAndTransactionViewsAgree` evaluates the full expression corpus (property-in, comparisons, count/not-exists patterns, endpoint equality, distinct endpoints, unsupported) through both views and requires identical results and error text; `BenchmarkConstraintContractEvaluation_EngineView` pins the count-pattern evaluation at 779–785 ns/op, 684 B/op, 12 allocs. Constraint tests green; storage ×2 green (86.2s), cypher green (35.1s); unique-constraint commit benchmark 9.6–10.0 ms/op unchanged in shape.
  - Progress (§6.1 aggregate discovery): probed `isAggregateExpression` (prefix-only) vs the span-based `findAggregateSpans` on nested aggregates (`round(sum(x) * 100) / 100`); the helper diverges on nested forms but execution flows through the span-based paths for the probed shapes, so no observable divergence exists in simple queries. Recorded as a known helper-vs-execution asymmetry; the prefix helper feeds fallbacks only.
	- Progress (HARD_CONVERGENCE.md item 5, MVCC head twins — the deferred last pair): the node/edge MVCC head writers and fresh-read loaders are now one generic implementation over `mvccHeadKeyLookup`/`mvccHeadKeyWriter` marker keyers: `loadMVCCHead[K]` (fresh-view read + decode + not-found mapping), `writeMVCCHeadInTxn[KL, KW]` (FloorVersion carry-forward via the SSI-isolated fresh read) and `writeMVCCHeadWithFloorInTxn[K]` (encode + key + set) — the six node/edge head functions are thin wrappers, so head semantics cannot drift between kinds. `BenchmarkBadgerEngine_CreateNode_MVCC` 23.9–24.3 µs/op, 8,434 B, 201 allocs before vs 23.5–24.4 µs/op, 8,434–8,436 B, 201 allocs after and `BenchmarkBadgerEngine_UpdateNode_MVCC` 16.2–16.3 µs/op, 178 allocs before vs 15.6–16.3 µs/op, 178 allocs after — identical within noise. Storage ×2 green (87.4s), cypher green (35.3s). MVCC version-record loaders: `loadNodeMVCCRecordExactInTxn`/`loadEdgeMVCCRecordExactInTxn` and `loadNodeMVCCRecordAtOrBeforeInTxn`/`loadEdgeMVCCRecordAtOrBeforeInTxn` are now one generic pair — `loadMVCCRecordExactInTxn[R, K]` and `loadMVCCRecordAtOrBeforeInTxn[R, K]` over a `mvccVersionKeyer` marker keyer with the record type and a plain decode function as parameters (allocation-free instantiations). `BenchmarkBadgerEngine_GetNodeVisibleAt_CurrentHeadGraphitiVector` 2,765–2,964 ns/op, 25 allocs before vs 2,697–2,914 ns/op, 25 allocs after — identical within noise. Storage ×2 green (89.8s), cypher green (35.6s). MVCC version writers: `writeNodeMVCCVersionInTxn`/`writeEdgeMVCCVersionInTxn` and the two tombstone writers are now one generic `writeMVCCVersionInTxn[R, K]` (record body + plain encode function, `versionKey` added to the `mvccVersionKeyer` keyer) — the four bodies become thin wrappers (−6 net lines). `BenchmarkBadgerEngine_CreateNode_MVCC` 23.3–23.5 µs/op, 201 allocs before vs 23.8–24.3 µs/op, 201 allocs after and `BenchmarkBadgerEngine_UpdateNode_MVCC` 16.3–16.4 µs/op, 178 allocs before vs 16.3–16.5 µs/op, 178 allocs after — within noise. Storage ×2 green (110.2s); cypher suite had one transient FAIL in a full run, then green ×2 consecutive (35.1–36.7s — unrelated to this storage-only change).
	- Progress (suite safety): `CompositeEngine.On*` event registration is now deterministic — `eventTargets` deduplicates constituents by underlying engine and a shared engine gets one wrapper that translates through every namespace view, so delivery no longer depends on constituent map iteration order (the `events_broadcast_to_constituents` subtest flaked in full-suite runs). New `TestCompositeEvents_SharedEngineTranslatesEveryNamespace` pins create/delete/edge events across both namespaces on a shared engine; storage suite passes ×2.
	- Progress (HARD_CONVERGENCE.md item 4, StreamNodes variants): the 24 StreamNodes* implementations across Badger/WAL/Async/Namespaced collapsed into one `StreamNodesWithOptions(ctx, StreamNodesOptions{Prefix, Projection, WithEmbeddings, ApplyDecayFilter, StripEmbeddings}, fn)` kernel per engine, plus a broadcast dispatcher on CompositeEngine. The five legacy public methods remain as thin wrappers for the consumers that still assert the legacy interfaces (cypher `match_multi`/`traversal`, `search/text_catalog`, `snapshot_stream`, size-tracking). The kernel is part of the `storage.Engine` contract, so delegation is unconditional on every engine (the `NodeOptionsStreamer` capability and its fallback branches are deleted); `pkg/nornicdb/embed_queue.go` migrated to the unified API, `sizeTrackingEngine` gained `StreamNodesWithOptions` with a compile-time assertion, and the storage/multidb test fakes model the new contract. `BenchmarkStreamNodes_(Full|ByPrefix|ByPrefixProjected)` measured identical within noise before vs after the consolidation (Full ~1.54–1.58 ms/op 16806 allocs, ByPrefix ~1.14 ms/op 15767 allocs, Projected ~1.10 ms/op 13767 allocs per 1000 nodes); `TestStreamNodesOptionsParityAcrossStacks` runs the same battery over Memory/Badger/WAL/Async/Namespaced/Composite.
	- Progress (wrapper contract): `storage.EngineUnwrapper` + `storage.UnwrapEngine` now provide the single canonical accessor for wrapped engines. The duplicate `GetEngine`/`GetUnderlying` accessors on Async/WAL and the `Unwrap`/`UnwrapEngine` variants were deleted and every accessor-variant type switch across cypher (`match`, `executor` ×2, `vector_registry`, `schema`, `reveal`, `transaction`), `storage/wal_ids`, `nornicdb/db`, `cmd/nornicdb/storage_metrics` and the GraphQL resolver now unwraps through the one interface. Layer-walk loops (`resolveWALAndDatabase`, `databaseName`) carry visited-set guards because `CompositeEngine.GetInnerEngine` returns the composite itself; the missing guard was a self-loop that stalled the cypher suite (caught by a 90s `-timeout` stack dump, fixed, full suite green). `TestWrapperDelegationContract_*` exercises every Engine method and every optional capability over Badger, WAL, Async, WAL+Async and Namespaced+WAL+Async stacks with a recording spy, asserting no `ErrNotImplemented` and delegation or documented composition. Gaps found and fixed: `NamespacedEngine.DeleteByPrefix` now performs a namespace-scoped delete instead of returning an unsupported error; `NamespacedEngine` gained `GetNodeLatestEffective`/`GetEdgeLatestEffective`; `AsyncEngine.On*` event registrations now forward to the inner notifier (they previously stored the callback and never fired); `AsyncEngine.Get(Node|Edge)LatestEffective` now overlay-checks the cache and delegates the persisted read to the inner `MVCCLatestEffectiveEngine`; `CompositeEngine` gained `Get(Node|Edge)LatestEffective`; `BadgerEngine.UpdateNodeEmbedding` now clears stored `EmbedMeta` when the incoming map is nil (retry-cleared embedding-failure markers persist). Compile-time assertions now pin `Engine` on every production engine type and `EngineUnwrapper` on every decorator.
		- Progress: unsupported `DROP` statements now return syntax errors, and missing INDEX/CONSTRAINT behavior has regressions. Full DDL/kernel contract coverage and benchmarks remain open.
- [ ] 8.2 Complete all remaining pinned-core TCK gaps and preserve Neo4j/Nornic extension suites.
- [ ] 8.3 Remove superseded handlers/evaluators/text-substitution paths; document retained optimizations and public API adapters.
- [ ] 8.4 Resolve every Cypher-relevant report candidate and document unrelated deferrals.

## 9. Qualify completion

- [ ] 9.1 Pass full tests, race suite, coverage gates, lint, build and scoped performance/retrieval checks.
	- Progress: cypher suite runtime cut from ~72s (121.6s observed under `make test`) to 35.5s. CPU profile attributed ~30% CPU plus most of the wall-clock to opening a fresh on-disk Badger database per route-convergence case in `newSetRouteServerStackExecutor`; the fixture now uses `NewBadgerEngineInMemory` with the same production Badger→WAL→Async→Namespaced chain (WAL stays on-disk with SyncMode none). `TestSetRoutesConverge` 7.03s→0.97s and `TestKeysReturnsEveryKeyInOneOrder` 11.56s→1.63s; the remaining top consumer is the intentional Graphify push-profile workload.
- [ ] 9.2 Emit redacted informational rejection reports and a reproducible optimization backlog.
- [ ] 9.3 Attach fixing commits/PRs and passing evidence to every scoped issue; verify closure criteria.
- [ ] 9.4 Update compatibility/parser-mode docs, public API examples and CHANGELOG; synchronize verified OpenSpec contracts.
- [ ] 9.5 Review final completion evidence, close verified issues through the normal repository workflow, and archive the completed program.
